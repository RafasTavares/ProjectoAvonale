@model ProjectAvonale.Models.RepositorioModel

@{
    ViewBag.Title = "BuscarRepositorio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>BuscarRepositorio</h2>


@using (Html.BeginForm("Buscar", "Repositorios", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>RepositorioModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })


        <div class="form-group">
            <label class="control-label col-md-2">Nome</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Buscar" class="btn btn-default" />
            </div>
        </div>
    </div>
}

@* PARTIAL VIEW *@

@Html.Partial("_Repositorios")